<Page
    x:Class="PatronageWP.AddPlacePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PatronageWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Loaded="Page_Loaded"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <TextBox Text="{Binding Name, Mode=TwoWay}" Header="Name:"/>
            <TextBox Text="{Binding Adress, Mode=TwoWay}" Header="Adress:"/>
            <TextBox Text="{Binding Latitude, Mode=TwoWay}" Header="Lat:"/>
            <TextBox Text="{Binding Longitude, Mode=TwoWay}" Header="Lon:"/>
            <CheckBox IsChecked="{Binding HasWifi, Mode=TwoWay}" Content="Has Wifi"/>
        </StackPanel>
    </Grid>
</Page>
